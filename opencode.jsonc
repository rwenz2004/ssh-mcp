// Added for testing purposes, requires docker compose to run
{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "ssh-mcp": {
      "type": "local",
      "command": [
        "ssh-mcp",
        "--host=root@x86-server",
        "--key=/home/ut006206@uos/.ssh/id_ed25519",
        "--timeout=60000"
      ],
      "enabled": true,
    },
  },
}
